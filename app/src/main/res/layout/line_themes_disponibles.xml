<?xml version="1.0" encoding="utf-8"?>
<LinearLayout android:id="@+id/relativeLayout1"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:padding="5dp"
    android:orientation="vertical"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <androidx.cardview.widget.CardView
        android:id="@+id/card_view_inner"
        android:layout_width="130dp"
        android:layout_height="130dp"
        android:layout_marginTop="3dp"
        app:cardCornerRadius="10dp"
        android:layout_gravity="center"
        app:cardElevation="10dp" >

        <ImageView
            android:layout_width="110dp"
            android:layout_height="110dp"
            android:id="@+id/img_themes_disponibles"
            android:layout_gravity="center"
            android:src="@mipmap/ic_launcher_80_foreground"
            tools:ignore="ContentDescription" />

    </androidx.cardview.widget.CardView>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="title theme"
        android:layout_marginTop="5dp"
        android:layout_below="@id/card_view_inner"
        android:layout_gravity="center"
        android:id="@+id/txtTitleThemes"
        android:textColor="@color/rose_bonbon"
        android:layout_marginBottom="11dp"
        android:textStyle="bold"
        />

</LinearLayout>